module.exports = function straightCheck(high, low) {
    // if high === 14 its an ace
    let low_straight 
    let straight

    if(high - low === 4) {
        straight = true
    } else if(high === 14 && )
}